// operadores binários: operando + operador + operando
10 + 10;

// operadores unários: operando + operador ou operador + operando
10++;
++10;

// ++ antes, soma antes
// ++ depois, soma depois

// operadores: + - * /